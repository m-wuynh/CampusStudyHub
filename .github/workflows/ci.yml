name: Build and check

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore
        run: dotnet restore StudyHub.Web/StudyHub.Web.csproj

      - name: Build
        run: dotnet build StudyHub.Web/StudyHub.Web.csproj --no-restore --configuration Release
